<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.example.project.filmapp.MyApplication"
        android:allowBackup="false"
        android:icon="@drawable/cinema"
        android:label="@string/app_name"
        android:roundIcon="@drawable/cinema"
        android:supportsRtl="true"
        android:theme="@style/Theme.FilmApp"
        android:usesCleartextTraffic="true">
        <activity android:name="com.example.project.filmapp.detail.DetailActivity"
            android:exported="true"/>
        <activity android:name="com.example.project.filmapp.home.HomeActivity"
            android:exported="true"/>
        <activity
            android:name="com.example.project.filmapp.splashscreen.SplashScreenActivity"
            android:theme="@style/SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>